# Production Procedures

This document centralizes the commands used for production build and dependency installation.

## Commands

```bash
yarn build
composer install --no-dev --optimize-autoloader --classmap-authoritative --no-interaction
```
